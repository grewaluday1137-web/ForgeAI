FROM node:20-alpine
WORKDIR /app

# Ensure we use legacy-peer-deps for TanStack query conflict
COPY package*.json ./
RUN npm install --legacy-peer-deps

COPY . .

EXPOSE 3000

# We bind to 0.0.0.0 so the host can access it
CMD ["npm", "run", "dev"]
